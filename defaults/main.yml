---
# defaults file for grub

# Values are written in /etc/default/grub.d/local.cfg, according to the
# variables here. Examples below are default values and should be placed in
# Ansible's group or host vars for modifications. Special values like true or
# false should be properly quoted.

# grub_config:
#   GRUB_DEFAULT: 0
#   GRUB_DISABLE_OS_PROBER: "false"
#   GRUB_DISABLE_SUBMENU: n
#   GRUB_GFXMODE: 640x480
#   GRUB_GFXPAYLOAD_LINUX: keep
#   GRUB_TIMEOUT: 5
#   GRUB_TIMEOUT_STYLE: hidden
#   GRUB_CMDLINE_LINUX_DEFAULT:
#     quiet
#     splash
